<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="C-odigosDiagrumius.DeleteForn"
             BackgroundColor="LightGray">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="10">
        <Frame BorderColor="Black"
               CornerRadius="100"
               HeightRequest="100"
               WidthRequest="100"
               HorizontalOptions="Center"
               VerticalOptions="Center">
            <Image Source="user_icon.png" />
        </Frame>
        <Frame BorderColor="Black"
               CornerRadius="20"
               Padding="20"
               Margin="10,20">
            <Label Text="Deseja excluir o fornecedor?"
                   FontSize="20"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   TextColor="Black"
                   HorizontalTextAlignment="Center" />
        </Frame>
        <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center" Spacing="20">
            <Button Text="sim" BackgroundColor="LightGreen" TextColor="Black" WidthRequest="100" />
            <Button Text="não" BackgroundColor="LightCoral" TextColor="Black" WidthRequest="100" />
        </StackLayout>
    </StackLayout>
</ContentPage>
